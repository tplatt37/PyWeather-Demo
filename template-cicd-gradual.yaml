AWSTemplateFormatVersion : '2010-09-09'
Transform: AWS::Serverless-2016-10-31
Description: Creates the PyWeatherDemo Lambda - a simple Python3.8 Lambda function that helps showcase the basic features of Lambda, but uses a more sophisticated rollout.

Parameters:
  SecretArn:
    Type: String
    Default: "arn:aws:secretmanager:REGION:ACCOUNTID:secret:SECRETNAME"
    Description: "Enter the ARN of the Secrets Manager Secret that holds the OpenWeatherMap API Key (You must create this yourself!)"

Resources:

  PyWeatherDemo:
    Type: AWS::Serverless::Function 
    Properties:
      CodeUri: src/
      Handler: app.lambda_handler
      Runtime: python3.9
      # The AutoPublishAlias term tells the framework to detect when the function changes, and then deploy it using the live alias. 
      AutoPublishAlias: live
      Policies:
        - AWSSecretsManagerGetSecretValuePolicy:        # Policy template with placeholder value
            SecretArn: !Ref SecretArn
      DeploymentPreference:
        # A CodeDeploy Application and DeploymentGroup will be created for this Lambda
        #The deployment configuration determines the rate at which your CodeDeploy application shifts traffic from the original version of the Lambda function to the new version. 
        # NOTE: This is a custom DeploymentConfig to do a 50/50 Canary for 5 minutes (good for demos!)
        # This is defined in pipeline/pipeline.yaml
        Type: FiftyFiftyCanary
        # Specifies Lambda functions for deployment lifecycle hooks
        Hooks:
          # Test the new version before we start allowing invocations
          PreTraffic: !Ref beforeAllowTraffic
          # Test the new verstion after we start allowing invocations
          PostTraffic: !Ref afterAllowTraffic
      Events:
        PyWeatherDemoGet:
          # This is what is known as an "Implicit API" in SAM. Stages will be Prod/Stage - no way to re-configure
          # https://github.com/aws/serverless-application-model/blob/master/docs/internals/generated_resources.rst#api
          #
          Type: Api 
          Properties:
            Path: /weather
            Method: get
            
  APIGWPermission:
    Type: AWS::Lambda::Permission
    Properties:
      FunctionName: !GetAtt PyWeatherDemo
      Action: lambda:InvokeFunction
      Principal: apigateway.amazonaws.com
      # Such as:
      # arn:partition:execute-api:region:account-id:api-id/stage/http-method/resource-path
      # NOTE: ServerlessRestApi is an implicit things created via SAM when you use an Event of type Api:
      # https://github.com/aws/serverless-application-model/blob/master/docs/internals/generated_resources.rst#api
      #
      # Run this via script?
      # aws lambda add-permission --function-name my-function --action lambda:InvokeFunction --statement-id sns-my-topic \
      # --principal sns.amazonaws.com --source-arn arn:aws:sns:us-east-2:123456789012:my-topic
      #from: https://docs.aws.amazon.com/lambda/latest/dg/access-control-resource-based.html#permissions-resource-cleanup
      SourceArn: !Sub 'arn:aws:execute-api:${AWS::Region}:${AWS::AccountId}:${ServerlessRestApi}/*/GET/weather'
      
  # Specifies the BeforeAllowTraffic lifecycle hook Lambda function
  beforeAllowTraffic:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: src-BeforeAllowTraffic/
      Handler: beforeAllowTraffic.handler
      Policies:
        - Version: "2012-10-17"
          # Grants this function permission to call codedeploy:PutLifecycleEventHookExecutionStatus        
          Statement: 
          - Effect: "Allow"
            Action: 
              - "codedeploy:PutLifecycleEventHookExecutionStatus"
            Resource:
              !Sub 'arn:aws:codedeploy:${AWS::Region}:${AWS::AccountId}:deploymentgroup:${ServerlessDeploymentApplication}/*'
        - Version: "2012-10-17"
          # Grants this function permission to call lambda:InvokeFunction        
          Statement: 
          - Effect: "Allow"
            Action: 
              - "lambda:InvokeFunction"
            Resource: !Ref PyWeatherDemo.Version
      Runtime: nodejs16.x
      # Specifies the name of the Lambda hook function      
      FunctionName: 'CodeDeployHook_beforeAllowTraffic_PyWeatherDemo'
      DeploymentPreference:
        Enabled: false
      Timeout: 5
      Environment:
        Variables:
          NewVersion: !Ref PyWeatherDemo.Version
          
  # Specifies the AfterAllowTraffic lifecycle hook Lambda function
  afterAllowTraffic:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: src-AfterAllowTraffic/
      Handler: afterAllowTraffic.handler
      Policies:
        - Version: "2012-10-17"
          Statement: 
          # Grants this function permission to call codedeploy:PutLifecycleEventHookExecutionStatus         
          - Effect: "Allow"
            Action: 
              - "codedeploy:PutLifecycleEventHookExecutionStatus"
            Resource:
              !Sub 'arn:aws:codedeploy:${AWS::Region}:${AWS::AccountId}:deploymentgroup:${ServerlessDeploymentApplication}/*'
        - Version: "2012-10-17"
          Statement: 
          # Grants this function permission to call lambda:InvokeFunction          
          - Effect: "Allow"
            Action: 
              - "lambda:InvokeFunction"
            Resource: !Ref PyWeatherDemo.Version
      Runtime: nodejs16.x
      # Specifies the name of the Lambda hook function      
      FunctionName: 'CodeDeployHook_afterAllowTraffic_PyWeatherDemo'
      DeploymentPreference:
        Enabled: false
      Timeout: 5
      Environment:
        Variables:
          NewVersion: !Ref PyWeatherDemo.Version

  # This is a simplified version of the PyWeather function 
  # used specficially for a Step Functions State Machine demo.
  PyWeatherJSON:
    Type: AWS::Serverless::Function 
    Properties:
      CodeUri: src-PyWeatherJSON/
      Handler: app.lambda_handler
      Runtime: python3.9
      Policies:
        - AWSSecretsManagerGetSecretValuePolicy:        # Policy template with placeholder value
            SecretArn: !Ref SecretArn

Outputs:
  # API Gateway endpoint as an output, for convenience
  PyWeatherDemoRESTApi:
    Description: "API Gateway endpoint URL for Prod stage - change the city as desired."
    Value: !Sub "https://${ServerlessRestApi}.execute-api.${AWS::Region}.amazonaws.com/Prod/weather?city=Scranton"
